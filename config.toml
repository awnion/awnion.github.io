# The URL the site will be built for
base_url = "https://awnion.github.io"
title = "awnion::pages"

theme = "terminimal"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# Optional: enable tags
taxonomies = [{ name = "tags" }]


[markdown]
bottom_footnotes = false

# Syntax highlighting: defining this table enables it.
[markdown.highlighting]
theme = "one-dark-pro"

[extra]
# Put all your custom variables here

# How <title> is composed: "combined" → "Page title | Site title", "page_only" → "Page title".
page_titles = "combined"

menu_items = [
    # each of these is optional, name and url are required
    # $BASE_URL is going to be substituted by base_url from configuration
    { name = "home", url = "$BASE_URL" },

    # tags should only be enabled if you have "tags" taxonomy
    { name = "tags", url = "$BASE_URL/tags" },

    # custom pages
    { name = "archive", url = "$BASE_URL/archive" },

    # set newtab to true to make the link open in new tab
    { name = "github", url = "https://github.com/awnion", newtab = true },
]

logo_text = "awnion::pages"

accent_color = "blue-auto"
background_color = "auto"
